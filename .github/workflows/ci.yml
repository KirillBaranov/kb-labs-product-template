name: CI

on:
  push:
    branches: [main]
  pull_request:
    types: [opened, synchronize, reopened]
  workflow_dispatch: {}

jobs:
  ci:
    uses: KirillBaranov/kb-labs-devkit/.github/workflows/ci.yml@main
    with:
      node-version: '20'
      run-coverage: true
